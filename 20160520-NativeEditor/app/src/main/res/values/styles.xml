<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <!--item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item-->
        <item name="colorPrimary">@color/mm_green</item>
        <item name="colorPrimaryDark">@color/mm_green_dark</item>
        <item name="colorAccent">@color/mm_green</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="CustomizedWidgetMaterialButton">
        <item name="android:background">@drawable/fab_background</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textAppearance">@style/TextAppearance.AppCompat.Button</item>
        <item name="android:minHeight">48dip</item>
        <item name="android:minWidth">88dip</item>
        <item name="android:focusable">true</item>
        <item name="android:clickable">true</item>
        <item name="android:gravity">center_vertical|center_horizontal</item>
    </style>

    <style name="FloatingActionButton" parent="CustomizedWidgetMaterialButton">
        <item name="android:background">@drawable/fab_background</item>
        <item name="android:layout_width">56dp</item>
        <item name="android:layout_height">56dp</item>
    </style>

    <style name="FloatingActionButton.Mini">
        <item name="android:layout_width">40dp</item>
        <item name="android:layout_height">40dp</item>
        <item name="android:layout_margin">8dp</item>
    </style>

    <style name="FloatingActionButton.Mini.Feature">
        <item name="android:background">@drawable/fab_feature_background</item>
    </style>

    <style name="FloatingActionButton.Mini.UndoRedo">
        <item name="android:background">@drawable/fab_undoredo_background</item>
    </style>

    <style name="FloatingActionButton.Mini.ReadFile">
        <item name="android:background">@drawable/fab_readfile_background</item>
    </style>

</resources>
